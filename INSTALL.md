# 安装与合并

本项目提供 Delegation Mode 的说明文档、任务胶囊模板、证据模板和可选的角色配置快照。它本身不提供代理调度、模型路由或后台运行时。

## 合并核心规则

1. 先定位要使用的目标配置：目标项目根目录的 `AGENTS.md`，或用户级的全局 `AGENTS.md`。如果文件已经存在，先在同一目录创建可恢复的备份，并确认备份内容可读。
2. 打开目标文件，把本项目根目录 `AGENTS.md` 中与 Delegation Mode 相关的规则合并进去。保留目标文件现有的项目约束、工具说明和其他有效内容；不要用整文件覆盖，也不要删除未参与本次安装的规则。
3. 如果目标项目需要局部生效，优先合并到该项目根目录；如果需要跨项目复用，再合并到全局文件。目标环境已有的指令优先级和冲突处理规则仍然有效，冲突应交给 Coordinator 明确处理。
4. 合并后检查：默认启用和一次性启动提示、Coordinator 的职责、四个角色边界、禁止递归委派、写入文件的唯一负责人、委派价值判断、任务胶囊字段，以及来源缺口门禁都仍然存在。

## 可选角色配置

如需使用 `agents/*.toml`，逐个审阅后复制到目标环境配置的代理目录（例如配置的 Codex home 下的 `agents` 目录）。这些文件是当前环境的配置快照，不是跨主机兼容性承诺：模型名称、推理档位、服务层级、沙箱能力和可用字段都可能随主机或版本变化。目标环境不支持的字段应按其文档调整或移除，不能因为快照看起来可用就假设它能加载。

官方配置通常区分用户级代理目录（如 `~/.codex/agents/`）和项目级代理目录（如项目的 `.codex/agents/`）；实际加载位置以目标宿主的文档和设置为准。当前快照中的 `service_tier = "fast"` 并非所有宿主都支持；若目标环境未列出该字段，应删除或按目标环境要求调整。

只安装实际需要的角色配置。没有配置加载能力时，仍可按 `README.md` 和任务胶囊手动执行协作约定。

如果目标目录存在 `AGENTS.override.md`，必须先检查它的优先级和内容。它可能覆盖同目录的 `AGENTS.md`，使刚合并的规则不生效，或产生未加载提示；合并后应以实际加载结果为准。

## 验证与回滚

安装后检查目标 `AGENTS.md` 可读，确认备份仍在，并用一个小任务验证：第一次助手回复的第一行是否严格为 `delegation mode is on`，任务胶囊是否包含全部必填字段，来源无法访问时是否会产生 Source Gap Assessment。

若合并造成冲突，先停止使用冲突部分，依据备份恢复目标文件，再缩小合并范围并由 Coordinator 做决定。回滚应恢复目标原文件；本项目的文档和源配置不应被修改来掩盖目标环境的冲突。

## 官方参考

- [Subagents 配置说明](https://learn.chatgpt.com/docs/agent-configuration/subagents)
- [AGENTS.md 配置说明](https://learn.chatgpt.com/docs/agent-configuration/agents-md)
