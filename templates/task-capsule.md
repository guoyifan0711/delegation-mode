# Task Capsule

把下面的胶囊交给一个且仅一个明确的子代理；需要并行时，为每个子任务分别填写一份。

Role: `explorer` / `researcher` / `worker` / `reviewer`

Objective:

Relevant context:

Applicable constraints and skills:

Scope / allowed files or sources:

Acceptance criteria:

Required return format:

Do not:

Stop condition:
